 #!/usr/bin/python3                                                                                 
  2 
  3 def print_reversed_list_integer(my_list=[]):
  4     if my_list:
  5         i = len(my_list) - 1
  6         while i >= 0:
  7             print("{:d}".format(my_list[i]))
  8         i--
#!/usr/bin/python3

def print_reversed_list_integer(my_list=[]):
    if my_list:
        i = len(my_list) - 1
        while i >= 0:
            print("{:d}".format(my_list[i]))
        i--
